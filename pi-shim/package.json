{
  "name": "@wwt/pi-shim",
  "version": "0.0.0",
  "private": true,
  "description": "Raspberry-Pi service that speaks the Anthropic Messages API shape but answers each call by running a fresh headless Claude Code session on a Pro/Max subscription. Lets the proxy's LLM_BASE_URL point here for free Sonnet/Opus prose.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "express": "^4.21.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
